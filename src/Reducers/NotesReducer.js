const initalState = {
    list: [
        {title: 'Tests', body: 'testando initalState reducer'}
    ]
}

export default (state = initalState, action) => {

    switch(action){
        case  'ADD_NOTE': 
        
        break;
    }

    return state;
}